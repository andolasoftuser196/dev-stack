{
  "name": "dx-devstack",
  "displayName": "dx dev-stack",
  "version": "1.0.0",
  "description": "Operate a dx development environment: run it, debug it, run branches in parallel, and give coding agents isolated sandboxes with policy guardrails.",
  "keywords": ["docker", "devstack", "dx", "worktree", "agent-sandbox", "local-development"],
  "license": "MIT",
  "userConfig": {
    "dx_root": {
      "type": "string",
      "title": "dev-stack directory",
      "description": "Absolute path to the dev-stack directory containing the dx script. Leave blank to search upward from the project root."
    }
  }
}
