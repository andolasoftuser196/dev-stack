# Present so `dx db:migrate` has something to run. The URL is assembled from the
# process environment in env.py rather than written here, because a committed
# connection string is one that points at the wrong instance.
[alembic]
script_location = alembic
