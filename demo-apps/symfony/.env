# Symfony reads this file itself. Everything that varies per machine or per
# instance — DB_*, REDIS_*, MAIL_* — is injected by dx as process environment
# and takes precedence, so it is deliberately absent here.
APP_ENV=dev
APP_DEBUG=1
APP_SECRET=dx-demo-not-a-secret
